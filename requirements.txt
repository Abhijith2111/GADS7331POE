# pygame-ce is a drop-in replacement for pygame with up-to-date pre-built
# wheels for current Python versions on Windows. `import pygame` still
# works exactly the same in our code.
pygame-ce>=2.5.0
ollama>=0.3.3
requests>=2.32.0
pydantic>=2.8.0
pytest>=8.3.0
